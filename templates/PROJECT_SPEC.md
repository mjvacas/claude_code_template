# PROJECT_SPEC template — copy to docs/PROJECT_SPEC.md, customize, delete this header.
# Optional; scale to project size. Pair with ARCHITECTURE.md (how) + BUILD_PLAN.md (order).
# ---

# <Project> — Spec

> The "why" and "what" — the source of truth for scope. Read at session start; don't
> re-litigate what's settled here (revisit via a new ADR).

## Mission

One paragraph: what this is, and the single outcome it exists to produce.

## Scope

- **In scope (v1):** …
- **Explicitly NOT in scope:** … (what you are deliberately not doing — as important
  as what you are; it's what stops scope creep).

## Success metrics

How you'll know it's working. Prefer measurable.

## Hard constraints

The non-negotiable invariants (mirror the code-guarded ones in CLAUDE.md).

## Operating principles

The handful of rules that resolve future judgment calls when the spec is silent.
