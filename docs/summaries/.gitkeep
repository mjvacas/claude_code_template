# Monthly compressed recaps live here as YYYY-MM.md. Archive old AI_CONTEXT.md
# session blocks into these when AI_CONTEXT.md grows long. This file only keeps the
# otherwise-empty directory in git; delete it once a real summary exists.
